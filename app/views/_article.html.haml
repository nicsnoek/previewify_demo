- article_classes = [(article.published? ? "published" : "preview")]
- article_classes << "unpublished_changes" if article.has_unpublished_changes?
.article{:class => article_classes.join(' ')}
  %h2.title
    = article.title
  .body_text
    = article.body_text


